class Kmean {
    // Decalre how many cluster you want (Declare K, K = 3 meaning I want 3 clusters)
    // Select init cluster
    // Measure the distance between the cluster
    // Calculate the mean of cluster
    // The variations have to be almost equal
    
}